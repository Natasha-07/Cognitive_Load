Based on the cross-validation means and the untouched final test-set results, the most defensible single-model choice is the MLP. It shows the strongest overall performance when considering both phases of evaluation together: it has the highest weighted final-test F1 and accuracy (weighted by test-set size), and it also leads on weighted cross-validation F1 and accuracy. While Random Forest is slightly stronger on the smaller real holdout subset, the synthetic holdout is much larger and provides more statistical evidence, where MLP performs best. This makes MLP the most justifiable selection for final reporting because it is consistently strong across development (5-fold CV) and independent validation (untouched test set), which aligns with standard model selection practice.